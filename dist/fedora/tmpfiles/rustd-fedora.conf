# Fedora services use these state directories before their package tmpfiles
# snippets are guaranteed to have been processed by the native boot graph.
d /var/lib/chrony 0750 chrony chrony -
d /var/log/chrony 0750 chrony chrony -
